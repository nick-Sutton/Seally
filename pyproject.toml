[project]
name = "sealy"
version = "0.1.0"
description = "A planning algorithm library for static and dynamic environments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "networkx[default]>=3.6.1",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "pygame>=2.6.1",
]
